APP_PORT=3000

# Base URL for Nuxt server routes (defaults to stub-backed `/api/` handlers).
# Keep the trailing slash and swap this to your real backend when ready.
NUXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/
